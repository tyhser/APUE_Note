#include "apue.h"
#include <errno.h>
#include <fcntl.h>
int main(int argc, char **argv)
{
	int fd;
	if (argc != 2)
		err_quit("usage: directory_name");
	if ((fd = open(argv[1], O_RDWR|O_APPEND, NULL)) < 0)
		err_sys("can't open %s", argv[1]);


	exit(0);
}
